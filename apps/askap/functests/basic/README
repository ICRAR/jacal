to run a basic test execute 

>./run.sh

This is a bash script
1) Reads a config file
2) Stars up the daliuge environment
3) stars up the daliuge daemons
4) unrolls a basic imaging graph

This is encapsultated in the logical graph basic_imge.json
